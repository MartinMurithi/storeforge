

TODOS:
1. Sub-domain and slug should be generated by system. Done


In tenant svc, tenant is created with tenant id. Now test using gateway
If linking user to tenant fails, delete the created tenant to avoid orphaned tenants
Create CRUD to for roles(for admin)
Give new jwt token that has tenant id and role

2.  Update JWT to include tenant ID.Figure out how tenant and user srv will interact.
3. Design landing page for storeforge.(Look for another name)
4. Design 3 sample themes and save them in DB.
5. Get tenant by ID or slug.